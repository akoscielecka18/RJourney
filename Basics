#1 
Initialize two vectors: a=[4,0.5, 7, 0.5] and b=[2,0.1,3, 5.2].
- Count the average for vector a

-Count the scalar product of a∘b.

-Count the weighted average, of the elements of vector a, where the weights are defined in vector b.

-Calculate the Euclidean distance between vectors a and b.
